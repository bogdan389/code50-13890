#include <stdio.h>
int main(void)
{

    /*escape sequence = character combination consisting of a backslash \
                        followed by a letter or a combination of digits.
                        They specify action withit a line or string of text.
                        \n = newline
                        \t = tab
    */

    printf("I like pizza!\n");
    return 0;
}
